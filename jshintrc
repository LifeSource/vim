{
    "maxerr"          : 50,           // Maximum error before stopping

    // Enforcing
    "quotmark"        : false,      // Quotation mark consistency                                                  :
    "latedef"         : true,        // Require variables/functions to be defined before being used

    "esnext"          : true,
    "undef"           : true,          // requires all non-global variables to be declared (prevents global leaks)
    "unused"          : true,         // Unused variables
    "strict"          : true,         // requires all functions to run in ES5 Strict Mode

    "expr"            : true,
    // Environments
    "devel"           : true,
    "browser"         : true,
    "jasmine"         : true,
    "jquery"          : true,
    "mocha"           : true,
    "module"          : true,
    "node"            : true,

    // Custom Globals
    "globals"         : {}        // additional predefined global variables
}
